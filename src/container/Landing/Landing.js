import React from 'react';
import cssClasses from './Landing.module.css';

const Landing = (props) => {
    return(
        <React.Fragment>
            LANDING
        </React.Fragment>
    );
}

export default Landing;